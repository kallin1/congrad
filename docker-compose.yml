services:
  db:
    image: mysql:8.0
    container_name: congrad-db
    environment:
      MYSQL_ROOT_PASSWORD: 0528
      MYSQL_DATABASE: congrad
      # user 계정은 사용하지 않을 것이므로 이 두 줄은 있어도 괜찮지만, 명확성을 위해 제거해도 좋습니다.
      MYSQL_USER: user
      MYSQL_PASSWORD: 0528
    ports:
      - "3306:3306"
    volumes:
      - db-data:/var/lib/mysql
    command: [
      "--character-set-server=utf8mb4",
      "--collation-server=utf8mb4_unicode_ci",
      # 🛠️ [핵심 수정] root 계정의 Access Denied 오류 해결을 위해 인증 플러그인 변경
      "--default-authentication-plugin=mysql_native_password" 
    ]

  backend:
    image: congrad-backend:latest
    container_name: congrad-backend
    depends_on:
      - db
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://db:3306/congrad?serverTimezone=Asia/Seoul&useSSL=false&allowPublicKeyRetrieval=true
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: 0528
    ports:
      - "8080:8080"
    build:
      context: ./backend
      dockerfile: Dockerfile

  frontend:
    image: congrad-frontend:latest
    container_name: congrad-frontend
    depends_on:
      - backend
    ports:
      - "3000:80"
    build:
      context: ./frontend
      dockerfile: Dockerfile

volumes:
  db-data: